# PostgreSQL-Verbindung für Drizzle.
# Lokales Beispiel; für Produktion durch eigene Credentials ersetzen
# (z. B. eine gehostete Instanz wie Neon oder Supabase).
DATABASE_URL="postgres://postgres:postgres@localhost:5432/workation_app"
